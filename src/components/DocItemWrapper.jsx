import React from 'react';

// This component is now replaced by the swizzled DocItem theme component
// See src/theme/DocItem/index.js for the actual implementation
const DocItemWrapper = ({ children }) => {
  return <>{children}</>;
};

export default DocItemWrapper;